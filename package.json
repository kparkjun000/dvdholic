{
  "name": "dvdholic",
  "private": true,
  "description": "Heroku build: frontend only (backend built by Gradle)",
  "scripts": {
    "build": "cd netplix-frontend && npm ci && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
